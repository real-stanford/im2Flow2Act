hydra:
  run:
    dir: ${oc.env:DEV_PATH}/im2flow2act/experiment/dataset_generation/${now:%Y-%m-%d_%H-%M-%S}

avaliable_gpu: [0]
buffer_path: /path/to/data_buffer
episode_start: null
episode_end: null
# object: ["blue cube.","drawer."] # red drawer; checker cloth
object: ["red mug."] # red drawer; checker cloth

